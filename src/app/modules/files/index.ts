import * as store from './store';
import * as services from './services';
import * as components from './components';

export const files_module = {
  components: components,
  services: services,
  store: store.store
}